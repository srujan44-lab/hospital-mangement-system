export interface patientType {
  name: string,
  email: string,
  mobileNumber: string,
}

export interface doctorType {
  name: string,
  email: string,
  mobileNumber: string,
  specialties: string,
}

export interface adminType {
  email: string,
}